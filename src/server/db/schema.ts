/**
 * Datenbankschema.
 *
 * Die Tabellen entstehen in Etappe 1 aus den Domaenenschemata. Diese Datei
 * ist bewusst der einzige Ort, an dem Tabellenstruktur definiert wird —
 * drizzle-kit leitet daraus die SQL-Migrationen ab.
 */

// Platzhalter, damit die Datei ein Modul bleibt, solange keine Tabelle
// definiert ist. Wird in Etappe 1 durch die echten Tabellen ersetzt.
export {};
